export interface TravelConditionData {
  condition: string;
  minutes: number;
  timeString: string;
}

export interface CityTrafficData {
  city: string;
  country: string;
  minutes: number;
  timeString: string;
}
